# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'

target 'swift-oc-demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for swift-oc-demo

  # Local Pods
  pod 'ObjCLib', :path => './'
  pod 'SwiftLib', :path => './'

  target 'swift-oc-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'swift-oc-demoUITests' do
    # Pods for testing
  end

end